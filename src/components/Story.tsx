export default function Story() {
	return (
		<div>
			<div>
				<img src="" alt="" />
				<span>{fullName}</span>
			</div>
			<p>{quote} - {fullName}</p>
			<ul>
				{achievements.map(a) => (

				)}
			</ul>
		</div>
	)
}